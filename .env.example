# ===== CONFIGURAÇÃO DE SEGURANÇA =====
# IMPORTANTE: Copie este arquivo para .env e configure com valores reais

# Porta do servidor
PORT=3000

# MongoDB - Use MongoDB Atlas em produção
MONGO_URI=mongodb://localhost:27017/whatsapp-chat

# JWT Secret - USE UMA CHAVE FORTE E ÚNICA!
# Gere com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
# NUNCA use "senha" ou "chave123" em produção!
JWT_SECRET=SUBSTITUA_POR_UMA_CHAVE_SEGURA_ALEATORIA_DE_64_CARACTERES

# Origens permitidas para CORS (separadas por vírgula)
ALLOWED_ORIGINS=http://localhost:3000

# Ambiente (development, production)
NODE_ENV=development

# ===== OBSERVAÇÕES DE SEGURANÇA =====
# 1. NUNCA commite o arquivo .env no Git
# 2. Use senhas fortes e únicas
# 3. Em produção, use HTTPS obrigatoriamente
# 4. Configure firewall para proteger MongoDB
# 5. Mantenha as dependências atualizadas (npm audit)
# 6. Faça backup regular do banco de dados
# 7. Use variáveis de ambiente em produção, não .env
# 8. Considere usar PM2 para gerenciar o processo
